# doc-service 依赖(Python 文档 RAG 子服务)
# 复用 memory-service 的 bge-large-zh(通过 HTTP /embed),不重复加载模型

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
python-dotenv>=1.0
python-multipart>=0.0.9
httpx>=0.27.0

# PDF 解析(保留标题层级 + 表格为 Markdown)
pymupdf4llm>=0.0.10
pymupdf>=1.24.0

# Chroma 向量库(共享 memory-service 的持久化目录,只读检索 + 通过 /embed 写入)
chromadb>=0.4.24

# MySQL(BM25 FULLTEXT 检索 + chunks 读写)
pymysql>=1.1.0

# MySQL 连接池(替代每次新建连接,省 TCP 握手开销)
dbutils>=3.1.0

# BM25 内存索引(jieba 中文分词 + rank_bm25 真 BM25,替代 MySQL FULLTEXT)
jieba>=0.42.1
rank_bm25>=0.2.2

# Rerank(bge-reranker-v2-m3,RERANK_ENABLED=true 时加载)
sentence-transformers>=2.7.0
